# RF CIP Mobile Field Control V1 — 개발 기준 (고정)

## 최상위 기준
`docs/MASTER_PROMPT.md` 가 이 저장소의 **최상위 개발 기준**이다.
본 파일 및 모든 코드/문서는 Master Prompt에 종속되며, 충돌 시 Master Prompt가 우선한다.
Master Prompt는 사용자의 명시적 지시 없이 수정하지 않는다.

## 절대 개발 원칙 (Master Prompt §1)
1. 복잡한 ERP처럼 만들지 않는다.
2. 현장관리자에게 중복입력을 요구하지 않는다.
3. 최초 현장설정은 상세하게, 일일입력은 극도로 단순하게.
4. 매일 동일한 값은 재입력시키지 않는다.
5. 기본값과 전일값을 최대한 재사용한다.
6. 정상상태보다 예외상태만 입력하게 한다.
7. 한 번 입력한 데이터를 모든 문서가 재사용한다.
8. AI가 추측해서 계약수량·기성·원가를 확정하지 않는다.
9. 수량과 금액 계산은 결정론적 코드로 처리한다.
10. 계약상대방에게 내부 원가가 절대 노출되지 않도록 **서버 수준**에서 권한을 분리한다.
11. 기존 데이터를 덮어쓰지 말고 변경이력과 버전을 보존한다.
12. 새 기능 추가보다 현장관리자의 터치 횟수 감소를 우선한다.

새 기능 추가 전 필수 질문:
> "이 기능이 현장관리자의 일일 입력시간이나 오류를 실제로 줄이는가?"
아니라면 추가하지 않는다.

## 코드에 고정된 불변 규칙
- **지층종류(GROUND_TYPE)를 하드코딩하지 않는다.** 현장별 사용자 정의. 마이그레이션에 '토사/풍화암/연암' 등을 넣지 않는다(시드 데이터 예외).
- **천공번호 형식을 강제하지 않는다.** 작업도면(PDF)에 표기된 원문을 그대로 저장한다.
  정렬·범위선택은 항상 `sort_key`(= `core.fn_natural_sort_key`) 로 한다. TS 구현과 규칙이 반드시 같아야 한다.
- **지반조건 = 조합 + 깊이 구조.** 텍스트 한 줄로 저장 금지.
- **계획수량 0인 지층을 지우지 않는다.** `ground_type.status='PROVISIONAL'` 로 보존한다.
  현장 여건에 따라 나중에 출현할 수 있으므로 근거를 남긴다.
- **범위 일괄적용·환산·공별입력·수량산출서 가져오기는 전부 미리보기 API를 먼저 거친다.**
  시스템이 임의 확정 금지(§11, §12). 가져오기 반영은 `approved: true` 명시 승인이 있어야만 실행된다.
- **도면 라벨 결합 임계값은 글자 크기 대비 비율로 둔다.** 절대 pt 값은 축척이 다른 도면에서 깨진다.
- **도면에 없는 천공번호를 자동 삭제하지 않는다.** 대조표를 보여주고 사람이 처리방식을 고른다(§8).
- **공수의 확정 기준은 도면 넘버링이다.** `연장 ÷ C.T.C` 는 참고값(`is_reference=true`)이며
  이것으로 공수를 확정하지 않는다. C.T.C 는 구간마다 달라진다(간격을 좁혀야 하는 구간이 생긴다).
  계산값과 실제 공수의 차이는 ERROR 가 아니라 INFO 다.
- **`실행가`·`내역서` 등 원가 시트는 가져오기 대상이 아니다.** `core` 에 들어가면 §29 위반이다.
- **본사 사전 업로드는 5종이다**(사용자 확인): 계약내역서 · 천공조서 · 수량산출서 · 공내역서 ·
  작업도면(평면도 넘버링). 현장설정 단계가 이 순서로 보여야 한다.
- **단가를 천공 한 공 한 공에 붙이지 않는다.** 단가는 계약내역서에 있다.
  천공종류를 내역 품목코드에 걸고 `core.v_hole_price` 로 해석한다.
  설계변경이 나면 현재 revision 의 같은 품목코드를 자동으로 따라간다.
  공별 예외 단가는 드문 경우에만 쓰고, 어디서 온 단가인지(`price_source`)를 함께 낸다.
- **계약방식은 월과 일 두 갈래다.** 월대·월급(MONTHLY) / 일대·일당(DAILY).
  기타(OTHER)는 일 단위로 계산한다. **월 단위가 기본**이라 월대·월급은 월액 전액(FIXED)이 기본이고,
  불가항력 등으로 일자 마감할 때만 PRORATED 로 지정하고 사유를 남긴다.
- **현장은 계획심도까지 갔는지만 체크한다.** 숫자를 다시 적지 않는다.
  계획심도는 천공조서에 있고 공은 평면도 넘버링으로 정해져 있다.
  못 간 공만 골라 **실제심도와 사유를 둘 다** 받는다(DB 제약). 실제심도가 없으면
  수량이 계획심도로 잡혀 과다계상된다. 미달 자체는 ERROR 가 아니라 INFO 다.
- **엑셀 숫자셀은 표기를 잃는다.** (`2.0` → `2`) 열 단위 소수자릿수를 추론해 원문을 복원한다.
  `core` 로직이 아니라 `domain/quantitySheet/cells.ts` 의 책임이다.
- **지층별 길이 합계 = 총 계획심도** 를 코드로 검증한다.
- **PRIVATE_COST는 DB 권한으로 차단한다.** 프론트 숨김만으로 구현 금지.
- **share 스키마(외부 공유)는 private_cost에 어떤 의존도 가질 수 없다.** 자동 테스트로 강제된다.
- **원본 데이터를 덮어쓰지 않는다.** revision + audit.change_log 로 보존.
- **금액/수량 계산은 SQL/TS 결정론적 코드**로만. LLM 계산 금지.
- **멱등키는 조회 전에 잠근다.** `pg_advisory_xact_lock` 없이 `findStored` 만 하면
  통신 복구 순간 같은 요청이 동시에 도착해 **둘 다 저장된다**(실제 재현됨).
  멱등키 기록에 `ON CONFLICT DO NOTHING` 을 쓰지 않는다. 충돌은 조용히 넘기지 않는다.
  재전송 테스트는 순차뿐 아니라 **동시**(Promise.all)로도 해야 한다.
- **단가·원가 집계 뷰는 현장관리자에게 GRANT 하지 않는다.** 증빙률만 예외이며 **건수만** 내보낸다.
- **영수증(category=RECEIPT)은 저장 즉시 HEAD_OFFICE_ONLY 다.** 그래서 현장관리자는
  `stored_file` 행을 볼 수 없고 직접 INSERT 할 수 없다. 정책을 풀지 말고
  '영수증을 붙인다'는 동작만 통과시키는 SECURITY DEFINER 함수를 쓴다.
- **가드는 조용히 교정하지 말고 명시적 위반은 거부한다.** 값을 먼저 되돌리면
  RAISE 가 죽은 코드가 되고, 호출자는 성공했다고 착각한다.
- **단가가 없는 항목을 0원으로 만들지 않는다.** 빠진 건수를 WARN 으로 알린다.
- **월대 환산일수를 코드에 박지 않는다.** 현장 파라미터(`MONTHLY_WORK_DAYS`, 기본 30)다.
- **자동계산 근거(calc_detail)의 금액은 문자열로 남긴다.** JSON 숫자는 큰 금액에서 오차가 난다.
- **plpgsql 에서 OUT 파라미터와 컬럼명이 겹치면 `#variable_conflict use_column`** 을 쓴다.
- **일일입력은 예외만 묻는다.** 계획심도와 같으면 다시 묻지 않고, 지반조건이 계획과 같으면 아무것도 저장하지 않는다(§1-6).
- **재전송은 항상 안전해야 한다.** 저장 API 는 `client_request_id` 로 멱등 처리한다.
  없으면 오프라인 큐 재전송 때마다 레미콘·공수가 두 배로 쌓인다.
- **기본 인원·장비 테이블에 단가 컬럼을 두지 않는다.** 단가는 `private_cost` 에만 있다(§29).
- **투입비의 기준은 인원 수가 아니라 공수다.** 출력일보 = 인원 × 공수(1/0.5/0),
  장비가동일보 = 대수 × 가동일수. 현금 지급 여부와 무관하게 1일 또는 0.5일이 반드시 남아야 한다.
  공수 0인 항목은 0원 행을 만들지 말고 계산에서 빼라.
- **노무·장비는 월급/월대인 경우도 있고 일자로 계산하는 경우도 있다.**
  불가항력이나 변수가 생기면 일자로 마무리한다. `pay_type`/`charge_type` 은 계약방식이고
  그 달의 정산방식(`FIXED`/`PRORATED`)은 따로 두며 사유를 남긴다.
- **월액 전액(FIXED) 배분의 합계는 월액과 정확히 같아야 한다.** 일자별 반올림만 하면
  몇 원이 어긋나고 그 차이가 기성까지 따라간다. 잔액은 마지막 날에 몰아준다.
- **원가 계산의 기준 단위는 '월'이다.** 월액 배분은 그 달 전체를 봐야 맞다.
  하루만 떼어 계산하지 않는다.
- **월 기준일수(`MONTHLY_WORK_DAYS`)를 코드에 박지 않는다.** 현장 파라미터다.
- **공정률 기준(`quantity_basis`)을 숨기지 않는다.** 계약수량이 없어 계획심도로 대신 계산했다면 그 사실을 함께 돌려준다.
- **날짜 단위 [아니오] 를 '모든 공이 다르다' 로 저장하지 않는다.** 예외는 적은 공만 예외다.
  화면 문구가 "비워두면 계획심도를 씁니다" 이면 서버도 정확히 그렇게 동작해야 한다.
- **작업일보·천공일지는 새로 묻지 않는다.** 이미 받아 둔 값을 모을 뿐이다(§33).
  `core.fn_daily_report` / `fn_hole_log` 는 `private_cost` 를 조회하지 않는다. 테스트가 강제한다.
- **천공일지는 천공조서와 같은 칸으로 낸다**(사용자 확인). 지층 칸은 현장이 정한 지층으로 만들고
  계획수량 0인 미확정 지층도 칸을 지우지 않는다. 지층합계 ≠ 계획심도면 그대로 드러낸다.
- **§35 작업도면 완료 = 천공일지 완료 = 수량산출 실적.** 같은 테이블을 두 번 세지 말고
  서로 다른 경로로 센 값을 맞춰 본다. 어긋나면 ERROR 다.
- **서비스워커 캐시 목록에 `app.js` 가 import 하는 모듈을 전부 넣는다.** 하나라도 빠지면
  통신 끊긴 곳에서 화면이 통째로 안 뜬다.
- **오프라인은 '앱이 열려 있는 상태' 만이 아니다.** 앱을 닫고 현장에서 다시 여는 경로
  (콜드스타트)까지 동작해야 한다. 통신 될 때 입력화면 자료를 미리 받아 둔다.
- **버튼을 눌렀는데 아무 일도 안 일어나게 두지 않는다.** 안 되는 이유를 말해준다.
- **특이사항은 ERP 가 아니다**(§31). 유형은 사용자 확정 8종(소음 민원발생/진동 민원발생/
  레미콘 수급 지연/가설휀스 간섭/작업부지 미조성/검측지연/지반조건 상이/기타)이다.
  기타는 내용 필수(DB 제약). 음성메모는 받지 않는다(사진·PDF만). 종결은 본사만.
  일일입력이 이미 받은 예외(레미콘 지연·장비대기·심도미달·지반)는 재입력시키지 않는다.
  모아서 보여줄 뿐이다. 사건번호(EV-YYYY-NNNN)는 현장 단위 잠금으로 채번한다.
- **기성가능액 ≠ 실제 제출 기성이다**(§37). 칸을 나누고, 초안은 저장하지 않으며,
  확정은 본사만 한다. 초안과 다른 금액을 제출하려면 사유가 있어야 한다(DB 제약).
  확정 시점의 근거를 snapshot 으로 얼려 둔다(§38).
- **물량 공정률과 금액 공정률은 같은 규칙으로 센다.** 한쪽만 날짜로 자르면
  같은 화면에서 어긋나 보이고 현장은 "시스템이 틀렸다"로 읽는다. 기간은 기성이 받는다.
- **금액은 원 단위 2자리로 맞춘다.** 수량(4자리) × 단가(2자리)를 그대로 두면 6자리가 된다.
- **이미 기성한 공은 다음 회차에서 뺀다.** 반려하면 다시 대상이 되고 누계에서도 빠진다.
- **본사 대시보드 함수(`fn_dashboard*`)는 PUBLIC REVOKE 후 본사에만 GRANT 한다.**
  원가 합계가 나오는 유일한 화면이다. 한 줄 목록에는 원가를 넣지 않는다(§39 상세에서만).
- **core/share 에 금액 컬럼을 더할 때는 "계약금액인가 내부원가인가"를 먼저 답한다.**
  허용목록 테스트가 즉시 깨지도록 되어 있다. 목록만 늘리지 말고 판단을 남겨라.

## 역할 (Master Prompt §44)
| 역할 | DB 역할 | 범위 |
|---|---|---|
| HEAD_OFFICE | `rfcip_head_office` | 전체 현장 + 전체 원가 + 승인 |
| FIELD_MANAGER | `rfcip_field_manager` | 배정 현장만. 계약단가 조회 가능(사용자 지시). 내부원가(private_cost 단가/집계) 접근 불가 |
| EXTERNAL | `rfcip_external` | `share` 스키마 뷰만. private_cost 접근 0 |

## 개발 순서 (Master Prompt §49)
PHASE 1 구조/DB/권한 ✅ → 2 SITE/CONTRACT/HOLE ✅ → 3 GROUND ✅ → 4 수량산출서 ✅ → 5 작업도면 ✅
→ 6 오늘작업입력 ✅ → 7 레미콘/인원/장비 ✅ → 8 비용/증빙/보안 ✅ → 9 작업일보/천공일지 ✅
→ 10 공정률/기성 ✅ → 11 SPECIAL_EVENT ✅ → 12 본사 대시보드 ✅ → 13 공유 분리 → 14 카카오톡 → 15 Pilot

각 Phase 종료 시 `docs/PHASE_XX_REPORT.md` 에 Master Prompt §50 의 10개 항목을 반드시 작성한다.

## 명령
```
npm --prefix server run db:reset   # 마이그레이션 + 시드 초기화
npm --prefix server run test       # 자동 테스트 (권한/검증 포함)
npm --prefix server run dev        # 개발 서버
```
